# set up your .venv with:
#   uv venv .venv --python 3.11
#   .venv/Scripts/python.exe -m pip install "fastapi" "uvicorn[standard]" "websockets"

# run backend (from project root):
#   .venv/Scripts/python.exe -m uvicorn app.main:app --port 9002 --reload
